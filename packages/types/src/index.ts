export interface AutomationKeyword {
  id: string;
  keyword: string;
  replyText: string;
  isActive: boolean;
}

export interface InstagramUser {
  id: string;
  username: string;
  accessToken: string;
}
